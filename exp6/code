%{
#include <stdio.h>
int change_int(const char *str) {
    int result = 0;
    int i = 0;
    while (str[i] != '\0') {
        result = (result * 10) + (str[i] - '0');
        i++;
    }
    return result;
}
%}

%%
[0-9]+  {int decimal_num = change_int(yytext);
        printf("%X", decimal_num);}

.       {ECHO;}
%%

int yywrap() {
    return 1;
}

int main() {
    printf("Enter: ");
    yylex();
    return 0;
}
